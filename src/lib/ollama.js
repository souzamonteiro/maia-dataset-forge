export async function ollamaJson({model,prompt,temperature=0.2,baseUrl='http://127.0.0.1:11434'}){const r=await fetch(`${baseUrl}/api/generate`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({model,prompt,stream:false,format:'json',options:{temperature}})});if(!r.ok)throw new Error(`Ollama ${r.status}: ${await r.text()}`);const j=await r.json();return JSON.parse(j.response)}
